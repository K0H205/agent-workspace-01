# Tasks

Cross-cutting task notes and briefs that span multiple service repositories.

Each task is a Markdown file (or a subdirectory) describing the goal, the
repos involved, and any context an agent needs to pick up the work. Service
code itself lives under `workspace/` (cloned, untracked) — link to it, don't
copy it here.
